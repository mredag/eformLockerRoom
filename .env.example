# Eform Locker Panel Configuration

# Server Configuration
NODE_ENV=production
PANEL_PORT=3003

# Security Configuration
COOKIE_SECRET=your-secure-cookie-secret-change-this
HTTPS_ENABLED=false

# Database Configuration
DATABASE_PATH=./data/eform.db

# Logging
LOG_LEVEL=info

# Authentication
SESSION_TIMEOUT=28800

# Hardware Configuration (for Kiosk)
MODBUS_PORT=/dev/ttyUSB0
MODBUS_BAUDRATE=9600

# Kiosk Configuration
KIOSK_ID=kiosk-1
ZONE=main
GATEWAY_URL=http://127.0.0.1:3000

# Maksisoft Integration Configuration (Panel Service)
MAKSI_BASE=https://eformhatay.maksionline.com
MAKSI_SEARCH_PATH=/react-system/api_php/user_search/users.php
MAKSI_CRITERIA_FOR_RFID=0
MAKSI_BOOTSTRAP_COOKIE=PHPSESSID=your-session-id; AC-C=ac-c
MAKSI_ENABLED=true
RFID_LOG_SALT=locker-system-salt-change-in-production

# Notes:
# - Set HTTPS_ENABLED=true only when running behind HTTPS proxy/load balancer
# - Change COOKIE_SECRET to a random string in production
# - Set NODE_ENV=development for local development
# - Set MAKSI_ENABLED=true to enable Maksisoft integration features
# - MAKSI_CRITERIA_FOR_RFID=0 is required for RFID searches in Maksisoft
# - Update MAKSI_BOOTSTRAP_COOKIE with valid session cookie from Maksisoft
# - Change RFID_LOG_SALT to a unique value for secure RFID hashing in logs
